import ClinicianLogin from "@/components/clinician/ClinicianLogin";

export const metadata = {
  title: "Clinician sign in — PhysioFlow",
  description:
    "Sign in to the full clinical record for every exercise, or request an account.",
};

export default function ClinicianLoginPage() {
  return <ClinicianLogin />;
}
