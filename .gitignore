node_modules/
.next/
out/
build/
*.tsbuildinfo
next-env.d.ts
.env*
.DS_Store

# large source models live in the GitHub release, not the repo
public/models/*.fbx
public/models/tex/

# Rendered exercise stills. Generated by scripts/thumbs.cjs, not authored —
# and a half-finished run must never be committed, because a grid of mixed
# stills and live figures looks broken.
/public/thumbs
